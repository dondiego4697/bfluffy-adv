<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>todo name</title>
        <link rel="stylesheet" href="#{config.bundlesUrl}bundle.css" />
    </head>
    <body>
        <div class="config-view" style="display:none;">#{clientConfig}</div>
        <div id="root"></div>
        <script defer src="#{config.bundlesUrl}bundle.js"></script>
    </body>
<html>
